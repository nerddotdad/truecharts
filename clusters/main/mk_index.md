---
title: Homelab cluster docs (redirect)
---

# Site home moved

The MkDocs **home page** is [`kubernetes/mk_index.md`](kubernetes/mk_index.md), not this file.

Edit `clusters/main/kubernetes/mk_index.md` for the docs homepage. Add other top-level tabs with `mk_*.md` in that same `kubernetes/` folder.
