machine:
  kubelet:
    extraConfig:
      # No taints - use node labels and affinity instead
      # Taints prevent other workloads from scheduling, which breaks DNS
      # Node labels allow affinity-based preferred scheduling instead
      registerWithTaints: []
