apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: node-config
  namespace: flux-system
spec:
  interval: 10m
  path: clusters/main/kubernetes/core/node-config/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: cluster

