---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: wizarr
  namespace: downloaders
spec:
  interval: 15m
  chart:
    spec:
      chart: wizarr
      version: 11.7.2
      sourceRef:
        kind: HelmRepository
        name: truecharts
        namespace: flux-system
      interval: 15m
  timeout: 20m
  maxHistory: 3
  driftDetection:
    mode: warn
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  values:
    image:
      repository: ghcr.io/wizarrrr/wizarr
      pullPolicy: IfNotPresent
      tag: latest@sha256:82f043a514c6055ad78c5ce468bb0382f4c578412c01bb97db18955b43b5c154
    persistence:
      database: # this is the target location for nzbget
        enabled: true
        type: nfs
        mountPath: /data/database
        path: ${NFS_WIZARR_DATABASE}
        server: ${NAS_IP}
    ingress:
      main:
        enabled: true
        ingressClassName: internal
        hosts:
          - host: wizarr.${DOMAIN_0}
            paths:
              - path: /
                pathType: Prefix
        integrations:
          certManager:
            enabled: true
            certificateIssuer: domain-0-le-prod
          traefik:
              enabled: false
          homepage:
            enabled: true
            name: Wizarr
            description: This is a wizarr instance
            group: "{{ .Release.Namespace }}"
            icon: ""
            widget: 
              custom:
                key: ${HP_WIZARR}
