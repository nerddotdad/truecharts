apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: matrix
  namespace: social
spec:
  interval: 5m
  chart:
    spec:
      chart: matrix-stack
      version: 0.6.1
      sourceRef:
        kind: HelmRepository
        name: ess-helm
        namespace: flux-system
  valuesFrom:
    - kind: ConfigMap
      name: matrix-values
