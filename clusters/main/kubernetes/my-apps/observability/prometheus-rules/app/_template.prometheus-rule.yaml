---
# TEMPLATE — copy to a new file (e.g. homelab-gaming.yaml), edit, add to kustomization.yaml, commit.
# Prometheus picks up PrometheusRule objects cluster-wide (ruleSelectorNilUsesHelmValues: false).
#
# Common label conventions:
#   severity: warning | critical
#   homelab_team: <your team label for routing (optional)>
#
# Templating (PrometheusRule):
#   alert:       stable snake_case ID — not templated (becomes alertname label).
#   summary:     one-line human title with {{ $labels.* }} / {{ $value }} → ntfy title via alertmanager-ntfy.
#   description:   responder detail; same Prometheus template vars (not Grafana {{ $values.A.Value }}).
#   https://grafana.com/docs/grafana/latest/alerting/fundamentals/templates/
#
# apiVersion: monitoring.coreos.com/v1
# kind: PrometheusRule
# metadata:
#   name: homelab-example
#   namespace: observability
#   labels:
#     app.kubernetes.io/part-of: homelab-observability
# spec:
#   groups:
#     - name: homelab.example
#       interval: 30s
#       rules:
#         - alert: example_high_memory
#           expr: |
#             (
#               container_memory_working_set_bytes{namespace="gaming", container!=""}
#               / on (namespace, pod, container) group_left()
#               kube_pod_container_resource_limits{resource="memory", namespace="gaming"}
#             ) > 0.9
#           for: 15m
#           labels:
#             severity: warning
#             homelab_team: gaming
#           annotations:
#             summary: "Container {{ $labels.namespace }}/{{ $labels.pod }} memory > 90% of limit ({{ $value | humanizePercentage }})"
#             description: "Investigate memory pressure before OOMKill."
#             # Tap link in ntfy (copy URL: python scripts/runbook_url.py HomelabExampleHighMemory)
#             runbook_url: https://docs.${DOMAIN_0}/main/kubernetes/my-apps/observability/runbooks/mk_runbook_homelab-example-high-memory/
