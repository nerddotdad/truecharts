# Intentionally broken HelmRelease for validating Flux/Helm alerts. Remove after testing.
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: alert-test
  namespace: flux-system
spec:
  interval: 10m
  path: clusters/main/kubernetes/my-apps/observability/alert-test/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: cluster
