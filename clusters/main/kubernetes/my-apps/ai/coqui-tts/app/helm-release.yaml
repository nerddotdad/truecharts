apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: coqui-tts
  namespace: ai
spec:
  interval: 15m
  chart:
    spec:
      chart: app-template
      version: 15.24.37
      sourceRef:
        kind: HelmRepository
        name: truecharts
        namespace: flux-system
      interval: 15m
  values:
    image:
      repository: ghcr.io/coqui-ai/tts
      pullPolicy: IfNotPresent
      tag: "latest"  # GPU version - pre-pull on node to avoid timeout
    
    securityContext:
      container:
        runAsNonRoot: false
        readOnlyRootFilesystem: false
        runAsUser: 0
        runAsGroup: 0

    podOptions:
      nodeSelector:
        kubernetes.io/hostname: k8s-worker-1
      runtimeClassName: "nvidia"
      tolerations:
        - key: gpu-workloads
          operator: Equal
          value: "required"
          effect: NoSchedule

    workload:
      main:
        podSpec:
          runtimeClassName: "nvidia"
          containers:
            main:
              enabled: true
              probes:
                startup:
                  enabled: true
                  type: http
                  port: 5002
                  path: /  # Flask web interface root endpoint
                  initialDelaySeconds: 120  # Coqui TTS needs time to download models on first run
                  periodSeconds: 10
                  failureThreshold: 30  # Allow up to 5 minutes for startup
                liveness:
                  enabled: true
                  type: http
                  port: 5002
                  path: /  # Flask web interface root endpoint
                  periodSeconds: 30
                  failureThreshold: 3
                readiness:
                  enabled: true
                  type: http
                  port: 5002
                  path: /  # Flask web interface root endpoint
                  periodSeconds: 10
                  failureThreshold: 3
              env:
                # Coqui TTS configuration - using faster models for better performance
                # VITS is faster than Tacotron2 while maintaining good quality
                TTS_MODEL: "tts_models/en/ljspeech/tacotron2-DDC"  # Can switch to "tts_models/en/ljspeech/vits" for faster synthesis
                TTS_VOCAB_MODEL: "vocoder_models/en/ljspeech/hifigan_v2"
              resources:
                requests:
                  memory: "2Gi"
                  cpu: "1000m"
                  nvidia.com/gpu: 1
                limits:
                  memory: "4Gi"
                  cpu: "2000m"
                  nvidia.com/gpu: 1
              command:
                - "/bin/bash"
                - "-c"
                - |
                  # Start Coqui TTS server
                  # The server will download models on first run
                  # Server binds to 0.0.0.0 by default
                  # Using VITS model for faster synthesis (much faster than Tacotron2)
                  tts-server --model_name tts_models/en/ljspeech/vits --port 5002

    persistence:
      models:
        enabled: true
        type: nfs
        mountPath: "/root/.local/share/tts"
        path: ${NFS_APP_CONFIGS}/coqui-tts/models
        server: ${NAS_IP}

    service:
      main:
        enabled: true
        ports:
          main:
            enabled: true
            protocol: tcp
            port: 5002
            targetPort: 5002

    ingress:
      main:
        enabled: true
        ingressClassName: internal
        hosts:
          - host: coqui-tts.${DOMAIN_0}
            paths:
              - path: /
                pathType: Prefix
        integrations:
          traefik:
            enabled: false
          certManager:
            enabled: true
            certificateIssuer: "domain-0-le-prod"

