# PostgreSQL credentials for Element CNPG
apiVersion: v1
kind: Secret
metadata:
  name: element-postgresql-credentials
  namespace: communication
type: kubernetes.io/basic-auth
stringData:
  username: synapse
  password: ${ELEMENT_DB_PASSWORD}
