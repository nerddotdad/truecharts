# NFS PersistentVolumeClaim for Element media storage
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: element-media-nfs
  namespace: communication
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 50Gi
  storageClassName: nfs-client
