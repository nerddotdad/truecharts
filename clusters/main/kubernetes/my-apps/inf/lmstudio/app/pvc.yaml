apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lmstudio-data
  namespace: inf
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  storageClassName: longhorn
