apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - immich/ks.yaml
  - immichframe/ks.yaml
  - jellyfin/ks.yaml
  - jellyfin-exporter/ks.yaml
  # Temporarily disabled — frees GPU time-slice for Ollama multi-agent workloads.
  # - owncast/ks.yaml
