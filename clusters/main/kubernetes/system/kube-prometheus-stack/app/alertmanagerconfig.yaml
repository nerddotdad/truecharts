---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/monitoring.coreos.com/alertmanagerconfig_v1alpha1.json
apiVersion: monitoring.coreos.com/v1alpha1
kind: AlertmanagerConfig
metadata:
  name: homelab
  namespace: kube-prometheus-stack
spec:
  route:
    groupBy: ["alertname", "namespace", "severity"]
    groupInterval: 10m
    groupWait: 2m
    receiver: homelab-webhook
    repeatInterval: 12h
    routes:
      - receiver: "null"
        matchers:
          - name: alertname
            value: InfoInhibitor
            matchType: =
      - receiver: "null"
        matchers:
          - name: alertname
            value: Watchdog
            matchType: =
  inhibitRules:
    - equal: ["alertname", "namespace"]
      sourceMatch:
        - name: severity
          value: critical
          matchType: =
      targetMatch:
        - name: severity
          value: warning
          matchType: =
  receivers:
    - name: "null"
    - name: homelab-webhook
      webhookConfigs:
        # In-cluster ntfy (topic: homelab-alerts). Subscribe via https://ntfy.${DOMAIN_0}/homelab-alerts
        - url: http://ntfy.observability.svc.cluster.local:10222/homelab-alerts
          sendResolved: true
          maxAlerts: 10
